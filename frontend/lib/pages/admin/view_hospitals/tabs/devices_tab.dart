import 'package:flutter/material.dart';

class HospitalDevicesTab extends StatelessWidget {
  const HospitalDevicesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Device management will be added once the /device API is exposed.',
      ),
    );
  }
}
